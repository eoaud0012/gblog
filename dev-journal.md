# dev-journal — gblog / damnote.com (포트폴리오 서사)

> 기술 기록은 `CLAUDE.md`, 이 문서는 "왜 그렇게 판단했는가"를 남긴다. 아래에 누적 추가.

## 2026-08-24 ~ 08-25 — 차단당한 트랙에서 자기 소유 자산으로

### 배경: 플랫폼 리스크를 구조로 푼 선택
네이버 블로그 자동 발행 계정이 검색 차단(blockType=3)을 맞았다. 흔한 대응은 "탐지를 더 잘 피하는 것"이지만, 그 길은 걸리면 계정이 아니라 사업(검색 유입=수익 전부)이 날아가는 게임이다. 그래서 문제를 **"감시를 어떻게 피할까"에서 "감시받을 행위를 안 하는 구조로 바꾸기"**로 재정의했다.
- 티스토리는 Open API 종료(2024)라 자동 발행 = 브라우저 봇 = 같은 리스크. 자가호스팅 워드프레스는 서버 비용과 운영 부담.
- **Hugo 정적 사이트 + GitHub Pages**를 택한 이유는 비용이 아니라 성질이다. 발행이 `git push`라 **봇 탐지라는 개념 자체가 성립하지 않는다.** 숨길 것이 없으면 탐지될 것도 없다.

### 자산을 내 것으로: 도메인이 플랫폼보다 먼저다
사용자가 "워드프레스가 아니라 깃헙 사이트네"라고 했을 때, 플랫폼 비교표를 주는 대신 **더 중요한 결정이 도메인이라고 말했다.** 호스팅은 나중에 옮겨도 되지만 `github.io` 주소로 색인을 쌓으면 이전 시점에 SEO 자산이 0으로 리셋된다. 도메인을 먼저 사면 플랫폼은 언제든 갈아탈 수 있다.
- 이름도 "살까노트" 같은 니치 종속형 대신 **범용(damnote.com)**을 권했다. 서브도메인으로 다른 프로젝트를 얹을 수 있고, 니치가 바뀌어도 자산이 따라온다.
- RDAP(등록부 공개 API)로 후보 20개를 즉시 조회해 9개 전멸 → 변형안에서 3개 확보. 도메인 탐색을 "검색해서 눈으로 보기"가 아니라 **프로그램으로 배치 조회**한 것이 시간을 줄였다.

### 봇 탐지 회사의 봇 탐지를 정면으로 만나다
Cloudflare 대시보드에서 브라우저 자동화가 Turnstile에 막혔다. 여기서 **Playwright로 더 세게 밀어붙이지 않았다** — Cloudflare는 그 탐지 기술을 파는 회사라 군비 경쟁이 무의미하고, 남은 단계 대부분이 어차피 사람 입력(비밀번호·결제·주소)이었다.
- 대신 **디버깅 포트로 띄운 실제 크롬에 CDP로 붙는 방식**으로 우회했다. 사람이 로그인한 세션을 그대로 쓰되 조작만 프로그램이 하는 형태. "자동화를 숨기는" 게 아니라 "사람이 한 로그인 위에서 반복 작업만 대신하는" 구조라 성질이 다르다.
- 이 패턴은 이후 쿠팡 파트너스에서도 그대로 재사용됐다(익스텐션이 도메인 차단되는 상황). **한 번 검증된 우회 구조를 자산으로 남긴 것.**

### 이관의 원칙: 재료는 가져오되 형태는 바꾼다
차단된 네이버 글 3편을 그대로 복붙하지 않았다. 원본이 살아 있으면 구글은 더 오래된 도메인을 원본으로 잡아 신생 사이트가 "복제본"으로 시작한다. 그래서 **원본 비공개 → 구글 검색 의도에 맞게 재구성**을 세트로 처리했다.
- 네이버식 형광펜·이모지·스캔형 문단을 걷어내고, 규정·정보 섹션을 앞에 배치(검색 의도), FAQ는 H3 질문형으로(스니펫 노림).
- **저작권 경계도 다시 그었다**: 판매자 상세페이지 사진은 전부 제외하고 자체 생성 차트만 사용. "쓸 수 있을 것 같다"가 아니라 "권리가 확인된 것만".

### 수익 배선에서 잡은 준법 결함
쿠팡 링크를 붙이고 나서 발견한 것: 블로그 글의 리뷰 수치(6,115건·평점 4.86)는 **네이버 스마트스토어** 값인데 링크는 **쿠팡**이었다. 판매처가 다르면 리뷰 수도 다르므로, 독자는 있지도 않은 수치를 쿠팡 리뷰로 읽게 된다.
- 본문에 "네이버 스마트스토어 기준"을 명시하는 것으로 해결했지만, 더 중요한 건 **규칙으로 승격**시킨 것이다: "링크 건 판매처의 수치만 그 판매처 것으로 제시한다." 이 규칙은 이후 유튜브 쇼츠에서 같은 계열 결함을 잡는 근거가 됐다.

### 실험이 끝나고 트랙을 접다
같은 날 네이버 "사람 발행 실험"이 blockType=3으로 나왔다. 계정 단위 분류가 확인되자 **새 계정 재시작을 권하지 않았다** — 같은 환경이면 결말이 같고, 이미 탐지 개념이 없는 대안을 확보했기 때문이다. 대신 그 실험 글(팩트체크를 거친 위탁수하물 정보글)을 damnote.com으로 이관해 여행준비 니치를 4편 중 3편으로 강화했다.
- **트랙 종료를 손실 확정이 아니라 자산 이동으로 처리**한 것. 죽은 채널에서 살릴 수 있는 건 콘텐츠지 채널이 아니다.

### 남은 기대치 관리
신규 도메인은 색인·순위까지 수개월이 걸린다. 그래서 "단기 수익 없음"을 전제로 주 2~3편 저속 운영을 설계에 박았다 — 대량 발행은 구글의 scaled content abuse 제재 대상이고, 그건 네이버에서 계정이 죽은 것과 같은 실패를 도메인 단위로 반복하는 일이다. **같은 실수를 다른 플랫폼에서 반복하지 않는 것**이 이 트랙의 첫 번째 성공 조건이다.

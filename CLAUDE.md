# CLAUDE.md — gblog (구글 타깃 제휴 블로그, 누적 지침 — 세션마다 아래에 추가)

네이버 블로그(`~/naver` naverauto)가 자동 발행 계정 검색 차단(blockType=3)을 맞아 만든 **대안 트랙**.
네이버 쪽 경위와 실측은 `~/naver/devlog.md` 2026-08-24 세션 D 참고. 네이버 트랙은 "사람 발행 실험" 결과에 따라 병행/중단 결정.

## 전략 (2026-08-24 확정)
- **플랫폼: Hugo 정적 사이트 + GitHub Pages(무료)**. 티스토리는 Open API 가 종료(2024)라 자동 발행 = 브라우저 봇 = 네이버와 같은 리스크.
  정적 사이트는 발행이 그냥 "내 사이트 배포"라 봇 탐지라는 개념 자체가 없음. 호스팅 비용 0.
- **검색: 구글·빙 타깃**. 구글은 AI 작성 자체를 제재하지 않고 품질만 봄(단, "scaled content abuse" 대량 저품질은 제재 — 발행량 절제와 품질 파이프라인은 네이버 때와 동일하게 유지).
- **수익: 쿠팡파트너스** 링크(가입은 사용자, 승인 후 pcid 필요) + 트래픽 생기면 애드센스 추가.
- **콘텐츠 파이프라인은 naverauto 재사용**: research→buyer_chain→writer→charts→factcheck 모듈을 가져오되, 출력만 Hugo 마크다운으로.
  네이버 SmartEditor 관련 코드는 전부 불필요.
- **기대치**: 신규 도메인은 구글 색인→트래픽까지 수개월. 단기 수익 없음을 전제로 저속 운영(주 2~3편).

## 승계 규칙 (naverauto 에서 그대로)
- LLM 은 `claude -p`(구독) 전용, env 에서 ANTHROPIC_API_KEY/AUTH_TOKEN 제거 후 호출. 유료 API 는 승인된 것만(ElevenLabs TTS 허용, 그 외 금지).
- 준법: 쿠팡파트너스 고지문 필수("이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다." — 공식 문구, 글 상단), 직접 안 써본 상품 실사용 단정 금지, 없는 수치 금지.
- 독자 보호: 규정·스펙 주장 팩트체크(공식 출처), 확인 못 하면 정직하게.
- 발행 후 실제 배포 페이지 열어 검증 후 완료 보고.

## 현재 상태 (2026-08-24 스캐폴드만)
- 폴더·git 만 생성. 다음 단계: (1) **사용자: 쿠팡파트너스 가입**(partners.coupang.com — 네이버/카카오 계정으로 가입, 승인까지 실적 심사 있음)
  (2) 사용자: GitHub 에 gblog 레포 생성 허락(eoaud0012) (3) Hugo 설치 + 사이트 골격 + 글 생성 파이프라인 이식 (4) 첫 글은 naver 쪽 "사람 발행 실험" 결과 본 뒤.

## 2026-08-24 세션 2 — 사이트 구축·첫 발행 (네이버 3건 이관)
- **결정(사용자)**: 네이버 검색 차단 3건을 구글용으로 재작성해 개시 콘텐츠로 사용, 네이버 원본은 비공개 전환(중복 콘텐츠 방지). 사람 발행 실험 결과를 기다리지 않고 gblog 착수로 변경.
- **구축**: Hugo 0.165 extended(winget, PATH 미반영 시 `$env:LOCALAPPDATA\Microsoft\WinGet\Links\hugo.exe`) + PaperMod(서브모듈). 블로그명 "살까 노트"(임시, 변경 가능). hugo.yaml: baseURL https://eoaud0012.github.io/gblog/, ko-KR, robots/sitemap on.
- **레포**: eoaud0012/gblog 은 스캐폴드 세션에 이미 생성돼 있었음(private) → public 전환 + Pages(build_type=workflow) 활성화. 배포는 .github/workflows/hugo.yaml(main 푸시 트리거). 라이브 확인 완료(글·차트 200).
- **글 3편** (content/posts/, 페이지 번들): power-bank-20000-flight / handheld-fan-cooling-pad / suitcase-size-guide. 날짜는 네이버 원 발행일 유지.
- **이관 규칙(향후 글에도 적용)**:
  - 상세페이지 사진(photos/) 사용 금지(저작권 — 판매자/제조사 소유). 자체 생성 차트(chart_*.png)만.
  - 제휴 링크·쿠팡 고지문은 승인 전이라 없음. 승인 후 pcid 링크+고지문 일괄 추가 예정.
  - "직접 사용 아님(후기 집계)" + "가격·수치는 수집 시점 기준" 고지는 모든 글 하단 유지.
  - 구조: 규정·정보 섹션(검색 의도)을 앞에, 제품 후기 분석은 뒤에. 형광펜(==)·이모지·네이버식 마커 제거. FAQ 는 H3 질문으로.
- **미완/다음 단계**: (1) ~~네이버 원본 3건 비공개~~ ✅ 8/25 완료 (2) ~~구글 서치콘솔 등록~~ ✅ 8/25 완료 (3) 쿠팡파트너스 가입(사용자) (4) 파이프라인 이식(신규 글 생성용) (5) 빙 웹마스터 등록(선택).

## 2026-08-25 세션 3 — 네이버 비공개·서치콘솔 완료, 저활동 모드 진입
- **사용자 방향**: 당분간 유튜브 집중, 블로그는 "씨만 뿌리고 방치"(신규 글 무기한 보류). 오라클 무료 ARM 서버는 별도 세션에서 생성 재시도 중(용량 부족) — 승인돼도 블로그 호스팅엔 불사용(예비/자동화용). 도메인 구입은 미결(구입 시 SEO 자산이 도메인에 쌓여 플랫폼 이전 자유로워짐 — 재권유 가치 있음).
- **네이버 원본 3건 비공개 전환 완료**(사용자 명시 승인 하): Playwright + `.browser` 프로필로 `postupdate?logNo=` 진입 → 발행 패널 `label[for=open_private]` 클릭 → 발행. 3건 모두 비로그인 접근 차단 검증(m.blog 페이지에 본문 컨테이너 부재). 스크립트: 세션 스크래치패드 naver_make_private.py (재사용 시 ~/naver 쪽에 복사 권장).
- **서치콘솔 등록 완료**(dtplanning0 계정, URL 접두어 속성): HTML 파일 인증(static/googleaafed9ca704bf87e.html — 삭제 금지), sitemap.xml 제출(23 URL, 제출 직후 "가져올 수 없음" 표시는 신규 속성 정상 — 수일 내 성공으로 바뀌는지 확인), 홈+글 3편 색인 생성 요청 완료.
- **장기 대기 상태**: 색인·트래픽은 수개월 단위. 다음 블로그 작업 재개 시 체크리스트 — 서치콘솔 색인 현황, 쿠팡 가입 여부, 사람 발행 실험 결과(네이버 채널 생사).

## 2026-08-25 세션 3 후반 — 커스텀 도메인 damnote.com 연결 완료
- **도메인**: damnote.com, Cloudflare Registrar 에서 구매(연 $10.46, 갱신 동일). 계정 eoaud0012@gmail.com (dtplanning0 아님 주의). 범용 이름 선정 이유: 향후 다른 프로젝트도 서브도메인으로 얹기 위해(SEO 자산이 도메인에 귀속 → 플랫폼 이전 자유).
- **DNS**(Cloudflare 존): A @ ×4 → 185.199.108~111.153, CNAME www → eoaud0012.github.io. **전부 DNS only(회색)** — GitHub 인증서 발급 조건, Proxied 로 바꾸지 말 것.
- **GitHub Pages**: cname=damnote.com, https_enforced=true (gh api). 사이트가 damnote.com **루트**로 서빙되므로 내부 링크에 /gblog/ 접두어 금지. baseURL=https://damnote.com/ 로 변경, 캐리어→보조배터리 내부 링크 수정. 기존 github.io URL 은 자동 301.
- **서치콘솔**: https://damnote.com/ 속성 신규 등록(기존 HTML 인증 파일로 자동 확인됨), sitemap.xml 제출 → 즉시 성공·23페이지 발견, 홈 색인 요청. github.io 속성은 301 덕에 자연 통합 — 삭제 불필요.
- **Cloudflare 자동화 실측**: 대시보드는 익스텐션 탭에서 Turnstile 봇 탐지에 걸림 → 별도 크롬(--remote-debugging-port=9333, 임시 프로필) 띄워 사용자가 로그인 후 Playwright connect_over_cdp 로 조작하는 패턴이 통함. DNS 폼은 커스텀 위젯이라 Name 입력 → Tab → 값 타이핑 방식이 안정적. 스크립트: 스크래치패드 cf_dns2.py.
- 사용자가 띄운 디버그 크롬 창은 닫아도 됨. 오라클 ARM 서버는 별도 세션에서 용량 대기 중(블로그와 무관).

## 2026-08-25 세션 3 마무리 — 쿠팡파트너스 수익 배선 완료
- 사용자가 이미 파트너스 가입돼 있었음(쿠팡 계정). 익스텐션은 coupang 도메인을 safety 차단 → **디버그 크롬(9333)+Playwright CDP** 패턴 재사용, 사용자 로그인 후 자동화 성공.
- 링크 생성 UI 실측: 상품 링크 검색 → 카드 **호버** 시 '상품정보/링크 생성' 버튼 → 클릭 → 3단계 페이지 HTML 에서 `link.coupang.com/a/...` 정규식 추출. 주의: `get_by_text("링크 생성")` 전역 검색은 상단 메뉴에 걸림 — 카드 좌표 호버 후 버튼 좌표 클릭이 확실.
- **삽입 완료**: 보조배터리 → https://link.coupang.com/a/guJQPMA6N2 (바우아토 포켓쉴, 쿠팡가 37,800원 동일 확인) / 선풍기 → https://link.coupang.com/a/guJVjkUm3U (알로코리아 FA3 냉각, 29,900원 동일). 두 글 상단에 공식 고지문, '가격과 구매 링크' 섹션(확인 날짜 명시). about 페이지 원칙 문구 갱신. 라이브 검증 완료.
- **비토 캐리어는 쿠팡 미입점**(브랜드 검색 결과 없음) → 준법 원칙(다른 제품 연결 금지)에 따라 링크 없이 정보성 글 유지. 추후 쿠팡 입점 제품 기준 재작업 가능.
- 파트너스 승인 구조 참고: 가입 직후 링크 생성 가능, **최종 승인은 실적 심사** — 초기 실적이 나와야 확정. 대시보드 '리포트'에서 클릭·수익 확인.

## 2026-08-25 세션 4 — 위탁수하물 글 이관 (네이버 트랙 종료 결정)
- **네이버 사람 발행 실험 결과**: 사람이 직접 발행한 새 글도 **blockType=3**. 7월 사람 발행 글은 0 → 자동 발행 시작(8/21) 이후 발행분만 전부 3. **계정 단위 분류로 결론**(상세·판독법은 ~/naver/devlog.md 2026-08-25 절).
- 그 실험 글(위탁수하물 금지 물품 정리)을 **damnote.com 으로 이관**: `content/posts/checked-baggage-prohibited-items/`.
  국토부(2026-04-08)·항공보안365·항공사 안내 기반, **제휴 링크 없는 정보성 글**. 위탁/기내 한 장 요약표 추가,
  보조배터리·캐리어 글과 상호 링크(여행준비 니치 강화). 네이버 원본은 중복 방지로 비공개 전환.
- 서치콘솔 색인 요청 완료. 이로써 글 4편(보조배터리·손풍기·캐리어·위탁수하물) 중 3편이 여행준비 카테고리.
- **주의**: 네이버 차단 글이 전부 비공개가 되어 blockType 관찰 대상이 남지 않았다. 나중에 계정 회복 여부를
  확인하려면 새 테스트 글을 1건 올려야 한다(10분). 결론이 이미 강해 당장은 불필요.

---

# 📌 다음 세션 인수인계 (2026-08-25 기준, 이 블록은 최신 상태를 덮어쓰며 갱신)

## 지금 상태 한 줄
damnote.com 라이브(글 4편·색인 요청 완료·쿠팡 링크 2개 배선). 네이버 종료. 유튜브 쇼핑 쇼츠 2편 렌더 완료 **업로드 승인 대기**.

## 자산 좌표 (헷갈리면 여기부터)
| 항목 | 값 |
|---|---|
| 블로그 | https://damnote.com (Cloudflare 도메인 + GitHub Pages, 레포 eoaud0012/gblog) |
| 도메인 계정 | Cloudflare **eoaud0012@gmail.com** (userEmail dtplanning0 과 다름 — 매번 헷갈림) |
| 서치콘솔 | damnote.com 속성(URL 접두어), 인증 파일 `static/googleaafed9ca704bf87e.html` **삭제 금지** |
| 쿠팡파트너스 | ID AF8552637 (2026-08-06 가입). API는 **누적 판매 15만원** 도달 시 열림 |
| 쇼핑 쇼츠 | **~/youtube** (여기 아님). 채널 "이거사도돼?" @igeosadodwae |
| 네이버 | maesoolee — 계정 분류(blockType=3), 발행 정지(PUBLISH_HOLD) |

## 다음 세션에서 바로 할 수 있는 것
1. **쇼츠 업로드**(승인 시): `cd ~/youtube && ./.venv/Scripts/python.exe shopping_render_main.py` — status를 approved로 되돌리고 YT_REFRESH_TOKEN_SHOPPING 을 비우지 않으면 자동 업로드된다. 업로드 후 **스튜디오에서 '유료 프로모션 포함' 수동 체크**(API 불가).
2. **새 글 쓰기**: 여행준비 니치(글 4편 중 3편). 후보 — 여행용 어댑터/변환플러그, 기내용 파우치, 여행자보험 체크리스트, 캐리어 무게 재는 법.
3. **쿠팡 링크 추가**: 캐리어 글은 비토가 쿠팡 미입점이라 링크 없음. 쿠팡 입점 프레임형 캐리어로 글을 손보면 3편 모두 수익화.
4. **애드센스**: 글 10편·트래픽 생긴 뒤 신청(지금은 이르다).

## 절대 하지 말 것 (실측으로 아픈 것들)
- 새로 뭔가 만들기 전에 **`~/` 프로젝트 폴더 전수 검사**. 2026-08-25에 쇼핑 쇼츠를 새로 만들었다가 ~/youtube 에 전부 있어서 반나절 날림.
- 다른 판매처 수치를 링크 건 판매처 것처럼 쓰지 말 것(네이버 리뷰 수 + 쿠팡 링크 = 오해 유발).
- 정확 가격을 영상에 박지 말 것(블로그는 수집일 명시하면 OK, 영상은 수정 불가라 구간만).
- 대량 발행 금지(구글 scaled content abuse). 주 2~3편 유지.

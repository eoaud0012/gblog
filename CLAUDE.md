# CLAUDE.md — gblog (구글 타깃 제휴 블로그, 누적 지침 — 세션마다 아래에 추가)

네이버 블로그(`~/naver` naverauto)가 자동 발행 계정 검색 차단(blockType=3)을 맞아 만든 **대안 트랙**.
네이버 쪽 경위와 실측은 `~/naver/devlog.md` 2026-08-24 세션 D 참고. 네이버 트랙은 "사람 발행 실험" 결과에 따라 병행/중단 결정.

## 전략 (2026-08-24 확정)
- **플랫폼: Hugo 정적 사이트 + GitHub Pages(무료)**. 티스토리는 Open API 가 종료(2024)라 자동 발행 = 브라우저 봇 = 네이버와 같은 리스크.
  정적 사이트는 발행이 그냥 "내 사이트 배포"라 봇 탐지라는 개념 자체가 없음. 호스팅 비용 0.
- **검색: 구글·빙 타깃**. 구글은 AI 작성 자체를 제재하지 않고 품질만 봄(단, "scaled content abuse" 대량 저품질은 제재 — 발행량 절제와 품질 파이프라인은 네이버 때와 동일하게 유지).
- **수익: 쿠팡파트너스** 링크(가입은 사용자, 승인 후 pcid 필요) + 트래픽 생기면 애드센스 추가.
- **콘텐츠 파이프라인은 naverauto 재사용**: research→buyer_chain→writer→charts→factcheck 모듈을 가져오되, 출력만 Hugo 마크다운으로.
  네이버 SmartEditor 관련 코드는 전부 불필요.
- **기대치**: 신규 도메인은 구글 색인→트래픽까지 수개월. 단기 수익 없음을 전제로 저속 운영(주 2~3편).

## 승계 규칙 (naverauto 에서 그대로)
- LLM 은 `claude -p`(구독) 전용, env 에서 ANTHROPIC_API_KEY/AUTH_TOKEN 제거 후 호출. 유료 API 는 승인된 것만(ElevenLabs TTS 허용, 그 외 금지).
- 준법: 쿠팡파트너스 고지문 필수("이 포스팅은 쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다." — 공식 문구, 글 상단), 직접 안 써본 상품 실사용 단정 금지, 없는 수치 금지.
- 독자 보호: 규정·스펙 주장 팩트체크(공식 출처), 확인 못 하면 정직하게.
- 발행 후 실제 배포 페이지 열어 검증 후 완료 보고.

## 현재 상태 (2026-08-24 스캐폴드만)
- 폴더·git 만 생성. 다음 단계: (1) **사용자: 쿠팡파트너스 가입**(partners.coupang.com — 네이버/카카오 계정으로 가입, 승인까지 실적 심사 있음)
  (2) 사용자: GitHub 에 gblog 레포 생성 허락(eoaud0012) (3) Hugo 설치 + 사이트 골격 + 글 생성 파이프라인 이식 (4) 첫 글은 naver 쪽 "사람 발행 실험" 결과 본 뒤.
